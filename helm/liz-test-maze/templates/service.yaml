apiVersion: v1
kind: Service
metadata:
  name: {{ include "liz-test-maze.fullname" . }}
  labels:
    {{- include "liz-test-maze.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liz-test-maze.selectorLabels" . | nindent 4 }}
